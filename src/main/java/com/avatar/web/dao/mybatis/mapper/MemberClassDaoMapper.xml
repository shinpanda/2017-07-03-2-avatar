<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.avatar.web.dao.MemberClassDao">
	<insert id="insert" parameterType="com.avatar.web.entity.MemberClass">
		insert into MemberRole(id,memberId,role)
		values((select ifnull(max(cast(id as unsigned)),0) + 1 from MemberRole r),#{param1}, #{param2});
	</insert>
</mapper>    